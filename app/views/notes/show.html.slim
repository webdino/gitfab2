- content_for :title do
  - @project.owner.name + "/" + @project.title +  " : Memo"

main#notes-show
  == render "projects/basic_informations"
  == render "notes/note_cards"
  == render "notes/templates"

- content_for :bottom do
  coffee:
    $ ->
      $("#facebook-wrapper").css "display", "none"
